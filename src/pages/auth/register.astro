---
import Layout from "../../layouts/Layout.astro";
import { AuthContainer } from "../../components/auth/AuthContainer";

export const prerender = false;
---

<Layout title="Rejestracja - 10x Cards">
  <div class="min-h-screen flex items-center justify-center bg-background py-12 px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-md">
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-foreground">10x Cards</h1>
        <p class="mt-2 text-muted-foreground">Utwórz konto, aby zapisywać i synchronizować swoje fiszki</p>
      </div>
      <AuthContainer client:load initialMode="register" />
    </div>
  </div>
</Layout>
<script>
  // Handle form switching and navigation
  document.addEventListener("DOMContentLoaded", () => {
    // This will be handled by the React components
  });
</script>
