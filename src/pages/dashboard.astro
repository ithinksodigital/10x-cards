---
import Layout from '../layouts/Layout.astro';
import { DashboardWrapper } from '../components/auth/DashboardWrapper';

// Mock user data - will be replaced with actual Supabase integration
const user = {
  id: 'mock-user-id',
  email: 'user@example.com',
  user_metadata: {
    full_name: 'Jan Kowalski',
    avatar_url: undefined,
  },
};
const session = null;
const isAuthenticated = true; // For dashboard, we assume user is authenticated
---

<Layout title="Dashboard - 10x Cards" user={user} session={session} isAuthenticated={isAuthenticated}>
  <DashboardWrapper client:load user={user} session={session} />
</Layout>
